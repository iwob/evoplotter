from distutils.core import setup

setup(name='evoplotter', version='0.9.0', packages=['evoplotter'], exclude_package_data={'': ['app', 'tests']},)

# Run the line below to install the package in the edit mode:
# python3 -m pip install -e <absolute path to the directory containing this path>
